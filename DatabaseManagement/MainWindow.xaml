<Window x:Class="DatabaseManagement.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:DatabaseManagement.Views"
        xmlns:local="clr-namespace:DatabaseManagement"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="700"
        DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">
    <TabControl ItemsSource="{Binding TabItems, Mode=TwoWay}">
        <!--<TabItem Header="Connect">
            <views:ConnectTab/>
        </TabItem>-->
        <!--TabItem Header="Find">
            <views:FindTab/>
        </>
        <TabItem Header="Add">
            <views:AddTab/>
        </TabItem-->
    </TabControl>
</Window>
